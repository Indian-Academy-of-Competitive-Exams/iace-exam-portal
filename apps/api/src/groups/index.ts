/**
 * The groups module's public surface (docs/03 §4.1). `group-rules` stays
 * private for the same reason `branch-rules` does.
 */
export { GroupsModule } from './groups.module';
export { GroupsService } from './groups.service';
